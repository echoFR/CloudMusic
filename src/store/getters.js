const getters={
    isShowHeader:(state)=>{
        return state.isShowHeader;
    },
    isShowLoading:(state)=>{
        return state.isShowLoading;
    },
    isShowMore:(state)=>{
        return state.isShowMore;
    },
    song:(state)=>{
        return state.song;
    }
}
export default getters;